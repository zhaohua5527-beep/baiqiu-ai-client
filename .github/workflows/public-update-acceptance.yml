name: Baiqiu public update acceptance

on:
  workflow_dispatch:

jobs:
  public-update:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Run full public update chain
        shell: pwsh
        run: ./resources/app/scripts/public-update-acceptance.ps1
